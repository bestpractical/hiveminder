<&| /_elements/wrapper, title => "History for '".$task->summary."'",
                        subtitle => "#".$task->record_locator &>

<& /task/_elements/transactions, task => $task &>

</&>

<%args>
$task
</%args>
